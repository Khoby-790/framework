Register page

    <%- include('../partials/messages')%>

            <form class="form-horizontal" action="/users/register" method="POST">
              
              <div class="form-group">
                <input type="text" value="<%= typeof name != 'undefined' ? name : '' %>" class="form-control" id="inputEmail3" placeholder="Full Name" name="name" required>
                <i class="fa fa-user"></i>
              </div>
              
              <div class="form-group">
                <input type="email" class="form-control" value="<%= typeof email != 'undefined' ? email : '' %>" placeholder="Email" name="email" required>
                <i class="fa fa-user"></i>
              </div>

      
              <div class="form-group help">
                <input type="password" value="<%= typeof password != 'undefined' ? password : '' %>" class="form-control" placeholder="Password" name="password" required>
                <i class="fa fa-lock"></i>
              </div>
              
              <div class="form-group help">
                <input type="password" class="form-control" value="<%= typeof password2 != 'undefined' ? password2 : '' %>" placeholder="Confirm Password" name="password2" required>
                <i class="fa fa-lock"></i>
              </div>
            
              <div class="form-group">
                <div class="flexbox align-items-center">
                  <button type="submit" class="btn theme-bg signUp">Sign Up</button>
                </div>
              </div>
            
            </form>